import { SetwidthDirective } from './setwidth.directive';

describe('SetwidthDirective', () => {
  it('should create an instance', () => {
    const directive = new SetwidthDirective();
    expect(directive).toBeTruthy();
  });
});
